apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mern-backend.fullname" . }}-test-connection"
  labels:
    {{- include "mern-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mern-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
